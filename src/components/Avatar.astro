---
export interface Props {
  url: string;
  alt?: string;
  style?: string;
}
const { url, alt, style } = Astro.props;
---

<img src={url} alt={alt} style={style} />

<style>
  img {
    border-radius: 50%;
  }
</style>
